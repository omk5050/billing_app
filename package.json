{
  "name": "billing-app",
  "version": "1.0.0",
  "description": "BillFlow – Full-stack billing application",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
